/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.google.co.uk", "https://cdn-icons-png.flaticon.com"],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
